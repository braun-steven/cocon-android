<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/apk/res-auto">

    <!--
    <item
        android:id="@+id/action_settings"
        android:orderInCategory="100"
        android:showAsAction="never"
        android:title="@string/action_settings" />
    -->

    <item
        android:id="@+id/edit_course"
        android:orderInCategory="100"
        tools:showAsAction="never"
        android:title="@string/edit_course_text"
        android:onClick="onEditCourseClick" />

    <item
        android:id="@+id/action_add_assignment"
        android:onClick="onClickAddAssignment"
        tools:showAsAction="always"
        android:title="@string/button_add_assignment" />
</menu>
